# Ralph Progress Log
Started: Wed Mar  4 05:16:21 PM -03 2026

## Codebase Patterns
- Form table exports (forms, formSubmissions, formFiles) had zero imports outside schema.ts — safe to remove without cascading changes
- pluginAssets table/type exports also had zero imports outside schema.ts and index.ts — same pattern, safe to remove
- Pre-existing typecheck errors exist in plugins/available/ and plugins/core-plugins/ (unrelated to schema)
- Test file `__tests__/services/forms.test.ts` uses raw SQL strings, not schema imports — not affected by schema table removal
- When removing a table that has active service code, refactor the service to use in-memory defaults rather than just deleting the table
- migrations-bundle.ts and migrations/*.sql always contain raw SQL `log_config`/`plugin_assets` strings — these are expected and not code dependencies
- `apiTokens` table exists in schema but has zero production queries — candidate for future cleanup
- Knip unused files/exports in plugins/ are expected false positives — plugins are dynamically loaded
- After editing migration .sql files, regenerate bundle with: `npx tsx packages/core/scripts/generate-migrations.ts`

---

## 2026-03-04 - US-001
- Removed forms, form_submissions, form_files table definitions from db/schema.ts
- Removed all related Zod schemas (insertFormSchema, selectFormSchema, etc.)
- Removed all related type exports (Form, NewForm, FormSubmission, etc.)
- Files changed: packages/core/src/db/schema.ts (103 lines deleted)
- **Learnings for future iterations:**
  - No code outside schema.ts imports the form tables — they were dead code
  - The forms.test.ts file uses raw SQL strings, not Drizzle schema imports, so it's unaffected
  - Typecheck has ~17 pre-existing errors in plugin files — ignore those
---

## 2026-03-04 - US-002
- Removed plugin_assets table definition from db/schema.ts (10 lines)
- Removed insertPluginAssetSchema, selectPluginAssetSchema Zod schemas (5 lines)
- Removed PluginAsset, NewPluginAsset type exports (2 lines)
- Removed pluginAssets, schema/type re-exports from index.ts (5 lines)
- Files changed: packages/core/src/db/schema.ts, packages/core/src/index.ts
- **Learnings for future iterations:**
  - migrations-bundle.ts contains raw SQL string referencing `plugin_assets` — this is expected and not a code dependency
  - Same removal pattern as US-001: check schema.ts + index.ts, grep confirms no other imports
---

## 2026-03-04 - US-003
- Removed log_config table definition from db/schema.ts (10 lines)
- Removed insertLogConfigSchema, selectLogConfigSchema Zod schemas (6 lines)
- Removed LogConfig, NewLogConfig type exports (2 lines)
- Removed logConfig + schema/type re-exports from index.ts (5 lines)
- Refactored logger.ts: replaced DB-based config queries with in-memory defaults
- Updated admin-log-config.template.ts to import LogConfig from services/logger instead of db/schema
- Files changed: schema.ts, index.ts, logger.ts, admin-log-config.template.ts
- **Learnings for future iterations:**
  - Unlike forms/pluginAssets, logConfig was actively queried by logger.ts and admin routes — required refactoring, not just deletion
  - Logger config is now in-memory with hardcoded defaults; updateConfig/getAllConfigs still work but don't persist
  - migrations-bundle.ts and migrations.ts reference `log_config` as raw SQL strings — these are DB-level references, not schema imports
  - No test files referenced logConfig directly
---

## 2026-03-04 - US-004
- Removed CREATE TABLE statements for forms, form_submissions, form_files, plugin_assets, log_config from 0001_initial_schema.sql
- Removed all related indexes (18 indexes), triggers (2 triggers), and seed data (log_config entries + default contact form)
- Regenerated migrations-bundle.ts via scripts/generate-migrations.ts
- Files changed: packages/core/migrations/0001_initial_schema.sql, packages/core/src/db/migrations-bundle.ts
- **Learnings for future iterations:**
  - Migration cleanup has 4 categories to check: CREATE TABLE, CREATE INDEX, CREATE TRIGGER, INSERT seed data
  - migrations-bundle.ts is auto-generated — run `npx tsx packages/core/scripts/generate-migrations.ts` after editing .sql files
  - After removing tables from migration, grep for table name to ensure no orphaned references remain
  - Build, typecheck, and tests all unaffected by migration SQL changes (they don't parse raw SQL)
---

## 2026-03-04 - US-005
- Verified all acceptance criteria for clean state after table removal
- Build: PASSES
- Tests: 1078 passed / 5 failed (all pre-existing) / 308 skipped — no new failures
- Typecheck: PASSES (only pre-existing plugin errors, none from schema changes)
- Knip: No new unused exports from schema removal; remaining are all plugin false positives
- Table audit: 10/11 remaining tables have production query references; apiTokens has none but is out of scope
- Files changed: scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - apiTokens table (api_tokens) has no production queries — future cleanup candidate
  - Most app code uses raw D1 SQL (db.prepare), not Drizzle query builder — grep for SQL table names, not just schema imports
  - Only logger.ts uses Drizzle ORM query builder (insert/select/delete with systemLogs)
  - Pre-existing test failures (5) and typecheck errors (plugins/) are unrelated to this work
---
